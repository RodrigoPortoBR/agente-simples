# ================================
# CONFIGURAÇÃO DO SISTEMA DE AGENTES
# ================================

# --------------------------------
# OpenAI (OBRIGATÓRIO)
# --------------------------------
OPENAI_API_KEY=OPENAI_API_KEY

# Modelo a usar (opcional - default: gpt-4o-mini)
# Opções: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Temperatura para respostas (0.0-1.0)
# 0.0 = determinístico, 1.0 = criativo
OPENAI_TEMPERATURE=0.7

# Tokens máximos por resposta
OPENAI_MAX_TOKENS=500

# --------------------------------
# Supabase (OBRIGATÓRIO)
# --------------------------------
SUPABASE_URL=SUPABASE_URL
SUPABASE_ANON_KEY=SUPABASE_ANON_KEY

# --------------------------------
# Memória e Conversação
# --------------------------------

# Número máximo de mensagens guardadas por sessão
MAX_HISTORY_MESSAGES=50

# Mensagens enviadas ao LLM como contexto
CONTEXT_WINDOW_MESSAGES=10

# Expiração de sessão (horas)
SESSION_EXPIRY_HOURS=24

# Nomes das tabelas no Supabase
CONVERSATION_TABLE=agent_conversations
CONVERSATION_MESSAGES_TABLE=agent_messages

# --------------------------------
# Performance
# --------------------------------

# Timeout para requisições HTTP (segundos)
REQUEST_TIMEOUT=30

# Máximo de tentativas em caso de erro
MAX_RETRIES=3

# TTL do cache (segundos)
CACHE_TTL_SECONDS=300

# --------------------------------
# Servidor
# --------------------------------

# Porta do servidor (default: 8000)
PORT=8000

# --------------------------------
# INSTRUÇÕES
# --------------------------------
# 1. Copie este arquivo para .env
# 2. Preencha os valores obrigatórios
# 3. Ajuste configurações opcionais conforme necessário
# 4. NUNCA commite o arquivo .env no Git!
#
# Para obter as credenciais:
# - OpenAI: https://platform.openai.com/api-keys
# - Supabase: https://app.supabase.com → Project Settings → API